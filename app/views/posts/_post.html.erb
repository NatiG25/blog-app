<div id="<%= dom_id post %>">

  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Text:</strong>
    <%= post.text %>
  </p>
  <% comments.each do |comment| %>
  <p>
  <%= render "comments/comment", comment: comment %>
 </p>
  <% end %>

</div>
